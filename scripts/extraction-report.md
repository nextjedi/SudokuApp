# BundledPuzzles Swift Extraction Report

Generated by `scripts/extract_swift_puzzles.py` for the iOS greenfield rebuild.

## Source
- Kotlin file: `shared/src/commonMain/kotlin/com/nextjedi/sudokustreak/puzzle/BundledPuzzles.kt`
- Parser entry forms supported: bare `"81-char"` and Kotlin Pair `"81-char" to 1.3f`
- Source had ratings: **No** (bare-string form only)

## Output
- Swift file: `scripts/BundledPuzzles.swift` (will be relocated to `iosApp/iosApp/Models/` during iOS rebuild)
- Size: **112,506 bytes** (1,227 lines)
- Format: `enum BundledPuzzles { static let easy/medium/hard/expert: [String] = [...] }`
  - Mirrors the existing `iosApp/iosApp/Engine/BundledPuzzles.swift` (`[String]` per difficulty), wrapped in an `enum` per task spec.
  - Includes `all` dictionary and a `bank(for:)` helper.
- Idempotent: re-running with no source change produces byte-identical output (timestamp derived from source mtime, not wall clock).

## Counts
- easy = 300
- medium = 300
- hard = 300
- expert = 300
- **Total = 1,200**

## Validation
Round-trip `--validate` flag re-parses the emitted Swift file:
- All 4 difficulties present
- 300 puzzles per difficulty
- Every string matches `^[0-9]{81}$`
- No duplicates within a difficulty
- Result: **OK: Swift output round-trips cleanly.**

## First puzzle per difficulty (40-char preview)
| Difficulty | First puzzle (truncated) | Rating |
|------------|--------------------------|--------|
| easy   | `0507030600070008000008160000000300000...` | (none in source) |
| medium | `0209000000480000310000630200094070030...` | (none in source) |
| hard   | `0802004005700001000023000008200900050...` | (none in source) |
| expert | `0830200900008001000293000080000987000...` | (none in source) |

## Warnings
None.

## Handoff notes for iOS greenfield agent
- Output is at `D:\Projects\SudokuApp\scripts\BundledPuzzles.swift`. Move it to `iosApp/iosApp/Models/BundledPuzzles.swift` (or wherever the new project structure places models).
- Source-of-truth remains the Kotlin file; regenerate via `py scripts/extract_swift_puzzles.py` after Kotlin updates.
- If iOS will eventually need ratings, extend the Kotlin source with `"..." to N.Nf` Pair syntax; the parser already detects this and switches output to `[(puzzle: String, rating: Double)]` tuples automatically.
